<!DOCTYPE html>
<html>
  <head>
    <title>Decompery</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag asset_path("favicon.png") %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-sky-900 text-zinc-200 flex flex-col justify-between h-screen">
    <header class="my-8 uppercase text-center">
      <span class="text-4xl">De</span><span class="text-3xl">co</span><span class="text-xl">mp</span><span class="text-base">ery</span>
    </header>
    <main class="container mx-auto mt-6 mb-auto px-5 flex flex-col">
      <% if notice.present? %>
        <p class="py-2 px-3 mb-5 text-emerald-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
      <% end %>
      <%= yield %>
    </main>
    <footer class="text-center my-4">
      Decomposed with 🍄 by <a href="https://maxveld.ink" class="text-emerald-400" target="_blank">Max VelDink</a> in Central Florida. Consider <a href="https://ko-fi.com/W7W563B5T" class="text-emerald-400" target="_blank">buying me a coffee</a>.
    </footer>
  </body>
</html>
